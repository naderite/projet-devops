apiVersion: 1

datasources:
  - name: Prometheus
    # Use the UID that the dashboard references to ensure provisioning matches
    # the dashboard. Consider changing this to a stable value (e.g. 'prometheus')
    # across environments for long-term stability.
    uid: PBFA97CFB590B2093
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
